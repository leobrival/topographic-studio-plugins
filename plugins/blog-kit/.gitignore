# Dependencies (should not exist, but ignore just in case)
node_modules/

# Generated by agents (do not commit)
.specify/research/
.specify/seo/
.specify/geo/
.specify/quality/
.specify/personas/
*.backup-*

# Article structure (commit selectively)
# Uncomment lines below to exclude from git:

# All articles (not recommended)
# articles/

# Only example articles (keep real articles)
articles/*/example-slug/

# Image backups (optional: exclude if storing externally)
# articles/**/images/.backup/

# Temporary image files
articles/**/*.tmp
articles/**/*.bak

# Optional blog constitution & personas (user choice to commit or not)
# Uncomment if you want to keep constitution private:
# .spec/blog.spec.json

# Personas - typically commit to share with team, but can exclude if private
# Uncomment to exclude personas from git:
# .spec/personas/
# Note: The schema and example are useful to commit for documentation

# Environment variables
.env
.env.*

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
logs/
*.log

# Claude Code plugins (should be in ~/.claude/plugins/)
plugins/
