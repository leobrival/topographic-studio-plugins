# Worktree Manager - Environment Variables Example
# Copy this file to .env if you need custom environment variables

# GitHub Configuration (optional, uses gh CLI by default)
# GITHUB_TOKEN=your_github_token_here

# Claude CLI Configuration (optional)
# CLAUDE_API_KEY=your_claude_api_key_here

# Custom Paths (optional, overrides config/default.json)
# WORKTREE_BASE_PATH=/custom/path/to/worktrees

# Debug Mode (optional, can also use --debug flag)
# DEBUG=true

# Terminal App (optional, overrides config)
# TERMINAL_APP=Hyper  # or iTerm2, Warp, Terminal

# Package Manager (optional, auto-detected by default)
# PACKAGE_MANAGER=pnpm  # or npm, yarn, bun
